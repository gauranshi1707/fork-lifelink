import { Droplet } from "lucide-react";
import { ComingSoon } from "./placeholders/ComingSoon";

const Donors = () => (
  <ComingSoon
    title="Nearby Blood Donor Finder"
    description="Post urgent requests, browse donors on a map, and connect through a privacy-first consent flow."
    icon={Droplet}
    accent="bg-emergency"
  />
);
export default Donors;
